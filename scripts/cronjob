# Run Spyderisk docs tree updater.
# Use either this, or the systemd equivalent, but not both.
# Check the paths and values for your system, don't just copy blindly!

# Run the script every 30 minutes

*/30 * * * * /usr/local/bin/check_new_tags.sh
